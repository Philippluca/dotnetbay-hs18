<Window x:Class="DotNetBay.WPF.SellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DotNetBay.WPF"
        mc:Ignorable="d"
        Title="BidView" Height="450" Width="800">
    <DockPanel>
        <Label FontSize="24" DockPanel.Dock="Top">Place Bid For Auction</Label>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Margin="8" Width="85" IsDefault="True">Place Bid</Button>
            <Button Margin="8" Width="85" IsCancel="True">Cancel</Button>
        </StackPanel>
        <Grid Height="Auto" Width="Auto">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Content="Title:"/>
            <Label Grid.Row="1" Grid.Column="0" Content="Image:"/>
            <Label Grid.Row="2" Grid.Column="0" Content="Description:"/>
            <Label Grid.Row="3" Grid.Column="0" Content="Start price:"/>
            <Label Grid.Row="4" Grid.Column="0" Content="Your Bid:"/>
            <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Title}"/>
            <Image Grid.Column="1" Grid.Row="1" Source="{Binding Image}" MaxHeight="100" MaxWidth="120"/>
            <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding Description}"/>
            <TextBlock Grid.Column="1" Grid.Row="3" Text="{Binding StartPrice, StringFormat=c}"/>
            <TextBox Grid.Column="1" Grid.Row="4"/>
        </Grid>
    </DockPanel>
</Window>
